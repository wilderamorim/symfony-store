{{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-lg btn-success">
        <span class="icon text-white-50">
          <i class="fas fa-save"></i>
        </span>
        {{ text }}
    </button>

{{ form_end(form) }}
{% extends 'base.html.twig' %}
{% block title %}Novo Usuário{% endblock %}

{% block body %}
    <section>
        <header class="border-bottom pb-2 mb-5 d-flex justify-content-between align-items-center">
            <h1 class="h4 mb-0">Novo Usuário</h1>
            <a href="{{ path('admin_users_index') }}" class="btn btn-primary btn-sm">
                <i class="fas fa-list"></i>
                Ver Usuários
            </a>
        </header>

        {% include 'admin/user/_form.html.twig' %}

    </section>
{% endblock %}

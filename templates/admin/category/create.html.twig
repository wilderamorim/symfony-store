{% extends 'base.html.twig' %}
{% block title %}Nova Categoria{% endblock %}

{% block body %}
    <section>
        <header class="border-bottom pb-2 mb-5 d-flex justify-content-between align-items-center">
            <h1 class="h4 mb-0">Nova Categoria</h1>
            <a href="{{ path('admin_categories_index') }}" class="btn btn-primary btn-sm">
                <i class="fas fa-list"></i>
                Ver Categorias
            </a>
        </header>

        {% include 'admin/category/_form.html.twig' %}

    </section>
{% endblock %}
